${artifactId}
=============
Acceptance tests for ${artifactId}.

Project Layout
==============
All tests are stored in ./src/main/fitnesse/FitNesseRoot/

Running tests
=============
To start the fitnesse instance you can use a maven profile:

mvn test -Pfitting
